# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

# Skill library

`.claude/skills/` holds this project's institutional knowledge — architecture, release pipelines, and every known failure signature with its proven fix. Start with `.claude/skills/README.md`. Before debugging anything, check the `troubleshooting` skill; before shipping, follow `verify`, then `release-android` / `release-pwa`.
